#include <stdio.h>int MDC(int a, int b) {  int resto = 1;  while(resto != 0) {    resto = a % b;    a = b;    b = resto;  }  return a;}int main() {  int numerador, denominador, mdc;  do {    printf("Digite os valores de numerado e denominador:\n");    scanf("%d %d", &numerador, &denominador);    if(numerador != 0 && denominador != 0) {      printf("fração inserida: %d/%d\n", numerador, denominador);      mdc = MDC(numerador, denominador);      numerador /= mdc;      denominador /= mdc;      printf("fração simplificada: %d/%d\n", numerador, denominador);    }  } while (numerador != 0 && denominador != 0);  return 0;}